# RoBotGap
Discord bot that comments league of legends matches

## Project structure
This project is split in two main parts:
 - `gapi/`: a reusable Python library for interacting with League of Legends data.
 - `bot/`: an application built on top of `gapi`, used for personal automation such as reacting to in-game events.